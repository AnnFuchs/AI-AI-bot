NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
NEXT_PUBLIC_USE_API_MOCKS=false

# Set to true only if backend auth uses HttpOnly cookies across origins.
# Backend CORS must allow credentials and the exact frontend origin.
NEXT_PUBLIC_API_INCLUDE_CREDENTIALS=false
